[project]
name = "ow"
version = "0.2.0"
description = "Overwatch 2 meta statistics web data generator"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.9",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
    "python-dotenv>=1.0",
    "requests>=2.28",
]
